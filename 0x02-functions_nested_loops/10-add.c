#include "main.h"
#include <stdio.h>

/**
* add - computes the result of the addition of 2 integers
*@a: right operand
*@b: left operand
* Return: returns the result
*/

int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
